import pkg from "casper-js-sdk";

console.log(
  "Deploy.makeDeploy =",
  pkg.Deploy.makeDeploy.toString()
);

console.log(
  "\nExecutableDeployItem.newModuleBytes =",
  pkg.ExecutableDeployItem.newModuleBytes.toString()
);

console.log(
  "\nExecutableDeployItem.standardPayment =",
  pkg.ExecutableDeployItem.standardPayment.toString()
);